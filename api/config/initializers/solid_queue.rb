# Solid Queue is the default job backend in Rails 8.
# In production, it uses the primary database (Cloud SQL).
# Workers are started by the Puma plugin (configured in config/puma.rb).
# No additional configuration needed for Cloud Run deployment.
